spring:
  config:
    activate:
      on-profile: dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://138.2.116.122:3306/dev_batch?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: portfolio
    password: moon0942!
